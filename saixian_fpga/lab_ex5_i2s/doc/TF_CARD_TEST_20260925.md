# 赛显 TF 卡测试清单（2026-09-25）

桌面素材已核对，未自动写入或格式化 TF 卡。
同一批 11 个文件已按 SHA-256 逐个校验后复制到工程的 `doc/TF卡上卡素材_20260925/`，在 GitHub 分支中以普通 Git 文件提供，克隆后可直接复制到 TF 卡，无须另行安装 Git LFS。素材共约 244 MB，请留意首次克隆时间和仓库体积。

| 用途 | 桌面文件 | 核对结果 |
| --- | --- | --- |
| 五图轮播 | `01.bmp`, `02.bmp`, `03.bmp`, `04.bmp`, `05-1.bmp` | 24 位无压缩 BMP；分辨率依次为 1280×720、1920×1080、1280×720、1280×720、640×480 |
| 百米结果背景 | `SAIXIAN_100M_1280x720_24bit.bmp` | 1280×720，带独立识别标记，不计入轮播五图 |
| 五首轮播音乐 | `BGM1.AUD` 至 `BGM5.AUD` | 均为 `SXAUD001`、48 kHz、16 位双声道、512 字节头部 |

只把上述 11 个文件放到**已备份的专用 TF 卡**，不要再放旧的 `BGM.AUD`、调亮版百米 BMP 或其他 BMP/AUD。读取器按物理扇区扫描，不读取文件名；在干净的卡上按 `01`～`05-1`、百米背景、`BGM1`～`BGM5` 的顺序逐个复制有助于形成预期顺序，但不能仅凭文件名保证物理顺序。旧文件即使从 FAT 目录删除，其头部仍可能留在物理扇区，因此不要用未经清理的旧卡判断扫描结果。不要运行会删除卡内文件的旧版 `sync_to_sd.py`。

测试时先确认五图、百米结果页及背景，再检查 BGM 自动播放；在轮播状态长按 K4 约 1 秒切下一首。每张图和每首歌都要逐项观察，尤其留意先前固定位置彩点是否重现。纯灰诊断位流已由用户实测无彩点，这把问题范围缩小到普通图像输出所涉及的上游链路，但尚未定位到具体模块。
